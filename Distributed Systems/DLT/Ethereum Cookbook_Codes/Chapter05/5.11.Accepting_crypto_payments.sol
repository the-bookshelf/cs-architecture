verifyTx(raw_transaction, transaction_index, merkle_sibling, block_hash);

relayTx(raw_transaction, transaction_index, merkle_sibling, block_hash, contract_address);

processTransaction(bytes rawTransaction, uint256 transactionHash) returns (int256)

